# dwains_theme
- !include
  - ../../../views/partials/heading.yaml
  - title: "Statistieken"
- type: markdown
  style: |
    ha-card {
      border-radius: 5px;
      background-color: var(--dwains-theme-primary);
    }
  content: >
    This is an house information addon. It can also parse some data from the global house information config.
    Don't believe it? Here it is: {{ (data | fromjson)['some_data'] }}<br>
    {{ (data | fromjson)['some_other_data'] }}
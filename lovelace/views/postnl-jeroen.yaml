title: PostNL Jeroen
path: postnl-jeroen
cards:
  - type: vertical-stack
    cards:
      - type: custom:decluttering-card
        template: quick-access-menu
        variables:
          - name1: Jeroen
          - icon1: mdi:account
          - path1: jeroen
      - type: custom:decluttering-card
        template: header
        variables:
          - content: '## Jeroen Klompen'
      # - type: custom:decluttering-card
      #   template: header
      #   variables:
      #     - content: '#### PostNL'
      # - type: custom:decluttering-card
      #   template: header
      #   variables:
      #     - content: '##### Other'
      # - type: horizontal-stack
      #   cards:
      #     - !include ../includes/blank-card.yaml
      #     - type: 'custom:decluttering-card'
      #       template: button
      #       variables:
      #         - entity: input_boolean.dummy1
      #         - name: Back to personal view
      #         - icon: mdi:robot-vacuum
      #         - show_state: false
      #         - show_icon: false
      #         - color: gray
      #         - opacity: 0.8
      #         - justify-self-name: center
      #         - padding-left-name: 0px
      #         - margin-top-name: 6px
      #         - margin-right: 0px
      #         - aspect_ratio: 6/1
      #         - tap_action:
      #             action: navigate
      #             navigation_path: /lovelace/jeroen
      #             haptic: light
      #         - hold_action:
      #             action: navigate
      #             navigation_path: /lovelace/jeroen
      #             haptic: light
      #     - !include ../includes/blank-card.yaml
      - type: custom:decluttering-card
        template: header
        variables:
          - content: '##### Brieven & pakketten'
      - type: horizontal-stack
        cards:
          - !include ../includes/blank-card.yaml
          - type: 'custom:postnl-card'
            style: |
              ha-card {
                background: var(--card-color-off);
                --paper-item-icon-color: var(--icon-color-off);
                color: var(--name-color-off);
                border-radius: {{ states('input_select.border_radius') }};
                box-shadow: {{ states('input_select.box_shadow') }};
                font-family: Helvetica;
                font-size: 12px;
                opacity: 0.8;
                box-shadow: none;
                }
            delivery: sensor.postnl_jeroen_delivery
            letters: sensor.postnl_jeroen_letters
            #hide:
            #  header: true
            #  delivered: false
            date_format: "DD MMM YYYY"
            time_format: "HH:mm"
            past_days: 14
          - !include ../includes/blank-card.yaml
      - !include ../includes/bottom-card.yaml